diff --git a/геном/выраженная_функция.gd b/геном/выраженная_функция.gd
new file mode 100644
index 0000000..e222806
--- /dev/null
+++ b/геном/выраженная_функция.gd
@@ -0,0 +1,49 @@
+class_name ВыраженнаяФункция
+extends RefCounted
+
+var тип: int = 0
+var органика: float = 0.0
+var пропорции: Vector3 = Vector3.ONE
+var параметры: Dictionary = {}
+var участки_генома: Array[Vector2i] = []
+
+
+func получить_параметр(имя: StringName, значение_по_умолчанию: Variant = null) -> Variant:
+	return параметры.get(имя, значение_по_умолчанию)
+
+
+func установить_параметр(имя: StringName, значение: Variant) -> void:
+	параметры[имя] = значение
+
+
+func содержит_параметр(имя: StringName) -> bool:
+	return параметры.has(имя)
+
+
+func получить_значение_поля(имя: StringName) -> Variant:
+	match имя:
+		&"органика":
+			return органика
+
+		&"пропорции":
+			return пропорции
+
+		_:
+			return параметры.get(имя)
+
+
+func содержит_поле(имя: StringName) -> bool:
+	return имя == &"органика" or имя == &"пропорции" or параметры.has(имя)
+
+
+func дублировать() -> ВыраженнаяФункция:
+	var копия := ВыраженнаяФункция.new()
+	копия.тип = тип
+	копия.органика = органика
+	копия.пропорции = пропорции
+	копия.параметры = параметры.duplicate(true)
+
+	for участок: Vector2i in участки_генома:
+		копия.участки_генома.append(участок)
+
+	return копия
diff --git a/геном/генетический_интерпретатор.gd b/геном/генетический_интерпретатор.gd
index 126378e..a5a979c 100644
--- a/геном/генетический_интерпретатор.gd
+++ b/геном/генетический_интерпретатор.gd
@@ -1,11 +1,22 @@
 extends RefCounted
 
 const КаноническаяПрограммаЭкспрессии = preload("res://геном/каноническая_программа_экспрессии.gd")
+const ВыраженнаяФункцияКласс = preload("res://геном/выраженная_функция.gd")
 
 
 static func выразить(геном: PackedByteArray, доступные_участки: Array[Dictionary]) -> Dictionary:
-	var выраженные_функции: Array[Dictionary] = []
-	var выраженные_начала: Dictionary = {}
+	var результат_нормализации: Dictionary = нормализовать_участки(геном.size(), доступные_участки)
+
+	if результат_нормализации.has("ошибка"):
+		return результат_нормализации
+
+	var участки: Array[Vector2i] = результат_нормализации["участки"]
+	return выразить_из_нормализованных_участков(геном, участки)
+
+
+static func нормализовать_участки(размер_генома_байт: int, доступные_участки: Array[Dictionary]) -> Dictionary:
+	var участки: Array[Vector2i] = []
+	var размер_генома_бит: int = размер_генома_байт * 8
 
 	for участок: Dictionary in доступные_участки:
 		if not участок.has("начало_бита") or not участок.has("длина_бит"):
@@ -13,8 +24,13 @@ static func выразить(геном: PackedByteArray, доступные_у
 				"ошибка": "Доступный участок должен содержать начало_бита и длина_бит."
 			}
 
-		var начало_бита: int = int(участок["начало_бита"])
-		var длина_бит: int = int(участок["длина_бит"])
+		if typeof(участок["начало_бита"]) != TYPE_INT or typeof(участок["длина_бит"]) != TYPE_INT:
+			return {
+				"ошибка": "Начало и длина доступного участка должны быть целыми числами."
+			}
+
+		var начало_бита: int = участок["начало_бита"]
+		var длина_бит: int = участок["длина_бит"]
 
 		if начало_бита < 0 or длина_бит <= 0:
 			return {
@@ -26,85 +42,143 @@ static func выразить(геном: PackedByteArray, доступные_у
 				"ошибка": "Доступные участки пока должны быть выровнены по границам байтов."
 			}
 
-		if начало_бита + длина_бит > геном.size() * 8:
+		if начало_бита + длина_бит > размер_генома_бит:
 			return {
 				"ошибка": "Доступный участок выходит за границы генома."
 			}
 
-		var начало_байта: int = начало_бита >> 3
-		var конец_байта: int = начало_байта + (длина_бит >> 3)
+		участки.append(Vector2i(начало_бита, длина_бит))
+
+	участки.sort_custom(сравнить_участки)
+
+	var объединенные: Array[Vector2i] = []
+
+	for участок: Vector2i in участки:
+		if объединенные.is_empty():
+			объединенные.append(участок)
+			continue
+
+		var последний_номер: int = объединенные.size() - 1
+		var последний: Vector2i = объединенные[последний_номер]
+		var последний_конец: int = последний.x + последний.y
+		var текущий_конец: int = участок.x + участок.y
+
+		if участок.x < последний_конец:
+			объединенные[последний_номер] = Vector2i(
+				последний.x,
+				max(последний_конец, текущий_конец) - последний.x
+			)
+		else:
+			объединенные.append(участок)
+
+	return {
+		"участки": объединенные
+	}
+
+
+static func выразить_из_нормализованных_участков(геном: PackedByteArray, участки: Array[Vector2i]) -> Dictionary:
+	var выраженные_функции: Array[ВыраженнаяФункция] = []
+
+	for участок: Vector2i in участки:
+		var начало_байта: int = участок.x >> 3
+		var конец_байта: int = начало_байта + (участок.y >> 3)
 		var позиция: int = начало_байта
 
 		while позиция < конец_байта:
-			var описание: Dictionary = КаноническаяПрограммаЭкспрессии.найти_по_маркеру(геном[позиция])
+			var маркер: int = геном[позиция]
+			var описание: Dictionary = КаноническаяПрограммаЭкспрессии.найти_по_маркеру(маркер)
 
 			if описание.is_empty():
 				позиция += 1
 				continue
 
-			var функция: Dictionary = {
-				"тип": описание["тип_функции"]
-			}
+			var длина_функции: int = КаноническаяПрограммаЭкспрессии.получить_длину_по_маркеру(маркер)
+
+			if длина_функции <= 0 or позиция + длина_функции > конец_байта:
+				позиция += 1
+				continue
+
+			var функция := ВыраженнаяФункцияКласс.new() as ВыраженнаяФункция
+			функция.тип = int(описание["тип_функции"])
+
 			var курсор: int = позиция + 1
 			var корректно: bool = true
+			var есть_органика: bool = false
+			var есть_пропорции: bool = false
 
 			for поле: Dictionary in описание["поля"]:
+				var имя := StringName(поле["имя"])
+				var значение: Variant
+
 				match поле["тип_данных"]:
 					КаноническаяПрограммаЭкспрессии.ТИП_UINT16:
-						if курсор + 1 >= конец_байта:
-							корректно = false
-							break
-
 						var сырое_значение: int = (геном[курсор] << 8) | геном[курсор + 1]
-						var значение: float = сырое_значение / float(поле["масштаб"])
-
-						if значение < float(поле["минимум"]):
-							корректно = false
-							break
-
-						функция[поле["имя"]] = значение
+						значение = сырое_значение / float(поле["масштаб"])
 						курсор += 2
 
-					КаноническаяПрограммаЭкспрессии.ТИП_VECTOR3_UINT16:
-						if курсор + 5 >= конец_байта:
+						if float(значение) < float(поле["минимум"]):
 							корректно = false
 							break
 
+					КаноническаяПрограммаЭкспрессии.ТИП_VECTOR3_UINT16:
 						var масштаб: float = float(поле["масштаб"])
-						var значение := Vector3(
+						значение = Vector3(
 							((геном[курсор] << 8) | геном[курсор + 1]) / масштаб,
 							((геном[курсор + 2] << 8) | геном[курсор + 3]) / масштаб,
 							((геном[курсор + 4] << 8) | геном[курсор + 5]) / масштаб
 						)
+						курсор += 6
 
 						var минимум: float = float(поле["минимум"])
+						var вектор: Vector3 = значение
 
-						if значение.x < минимум or значение.y < минимум or значение.z < минимум:
+						if вектор.x < минимум or вектор.y < минимум or вектор.z < минимум:
 							корректно = false
 							break
 
-						функция[поле["имя"]] = значение
-						курсор += 6
-
 					_:
 						корректно = false
 						break
 
-			if not корректно:
-				позиция += 1
-				continue
+				match имя:
+					&"органика":
+						if typeof(значение) != TYPE_FLOAT:
+							корректно = false
+							break
 
-			функция["участки_генома"] = [{
-				"начало_бита": позиция * 8,
-				"длина_бит": (курсор - позиция) * 8
-			}]
+						функция.органика = float(значение)
+						есть_органика = true
 
-			if not выраженные_начала.has(позиция):
-				выраженные_функции.append(функция)
-				выраженные_начала[позиция] = true
+					&"пропорции":
+						if typeof(значение) != TYPE_VECTOR3:
+							корректно = false
+							break
+
+						var вектор_значения: Vector3 = значение
+						функция.пропорции = вектор_значения
+						есть_пропорции = true
+
+					_:
+						функция.установить_параметр(имя, значение)
 
+			if not корректно or not есть_органика or not есть_пропорции:
+				позиция += 1
+				continue
+
+			функция.участки_генома.append(Vector2i(
+				позиция * 8,
+				(курсор - позиция) * 8
+			))
+			выраженные_функции.append(функция)
 			позиция = курсор
 
 	return {
 		"выраженные_функции": выраженные_функции
 	}
+
+
+static func сравнить_участки(левый: Vector2i, правый: Vector2i) -> bool:
+	if левый.x == правый.x:
+		return левый.y < правый.y
+
+	return левый.x < правый.x
diff --git a/геном/каноническая_программа_экспрессии.gd b/геном/каноническая_программа_экспрессии.gd
index 5a986ed..8e45696 100644
--- a/геном/каноническая_программа_экспрессии.gd
+++ b/геном/каноническая_программа_экспрессии.gd
@@ -178,17 +178,65 @@ const ОПИСАНИЯ_ФУНКЦИЙ: Array[Dictionary] = [
 ]
 
 
-static func найти_по_маркеру(маркер: int) -> Dictionary:
+static var описания_по_маркеру: Dictionary = {}
+static var описания_по_типу_функции: Dictionary = {}
+static var длины_по_маркеру: Dictionary = {}
+
+
+static func _static_init() -> void:
 	for описание: Dictionary in ОПИСАНИЯ_ФУНКЦИЙ:
-		if описание["маркер"] == маркер:
-			return описание
+		var маркер: int = int(описание["маркер"])
+		var тип_функции: int = int(описание["тип_функции"])
+
+		if описания_по_маркеру.has(маркер):
+			push_error("Повторное описание маркера 0x%02X." % маркер)
+			continue
+
+		if описания_по_типу_функции.has(тип_функции):
+			push_error("Повторное описание функции типа 0x%02X." % тип_функции)
+			continue
+
+		var длина_байт: int = рассчитать_длину_функции(описание)
+
+		if длина_байт <= 0:
+			continue
+
+		описания_по_маркеру[маркер] = описание
+		описания_по_типу_функции[тип_функции] = описание
+		длины_по_маркеру[маркер] = длина_байт
+
+
+static func найти_по_маркеру(маркер: int) -> Dictionary:
+	if описания_по_маркеру.has(маркер):
+		return описания_по_маркеру[маркер]
 
 	return {}
 
 
 static func найти_по_типу_функции(тип_функции: int) -> Dictionary:
-	for описание: Dictionary in ОПИСАНИЯ_ФУНКЦИЙ:
-		if описание["тип_функции"] == тип_функции:
-			return описание
+	if описания_по_типу_функции.has(тип_функции):
+		return описания_по_типу_функции[тип_функции]
 
 	return {}
+
+
+static func получить_длину_по_маркеру(маркер: int) -> int:
+	return int(длины_по_маркеру.get(маркер, 0))
+
+
+static func рассчитать_длину_функции(описание: Dictionary) -> int:
+	var длина_байт: int = 1
+
+	for поле: Dictionary in описание["поля"]:
+		match поле["тип_данных"]:
+			ТИП_UINT16:
+				длина_байт += 2
+
+			ТИП_VECTOR3_UINT16:
+				длина_байт += 6
+
+			_:
+				push_error("Неизвестный тип данных поля «%s»." % поле["название"])
+				return 0
+
+	return длина_байт
diff --git a/геном/канонический_компилятор_генома.gd b/геном/канонический_компилятор_генома.gd
index cc8dade..3060eff 100644
--- a/геном/канонический_компилятор_генома.gd
+++ b/геном/канонический_компилятор_генома.gd
@@ -3,28 +3,30 @@ extends RefCounted
 const КаноническаяПрограммаЭкспрессии = preload("res://геном/каноническая_программа_экспрессии.gd")
 
 
-static func скомпилировать(описание_клетки: Array[Dictionary]) -> Dictionary:
+static func скомпилировать(описание_клетки: Array[ВыраженнаяФункция]) -> Dictionary:
 	var геном := PackedByteArray()
 
 	for номер: int in range(описание_клетки.size()):
-		var функция: Dictionary = описание_клетки[номер]
+		var функция: ВыраженнаяФункция = описание_клетки[номер]
 
-		if not функция.has("тип"):
+		if функция == null:
 			return {
-				"ошибка": "У функции с номером %d отсутствует тип." % номер
+				"ошибка": "Функция с номером %d не задана." % номер
 			}
 
-		var описание: Dictionary = КаноническаяПрограммаЭкспрессии.найти_по_типу_функции(функция["тип"])
+		var описание: Dictionary = КаноническаяПрограммаЭкспрессии.найти_по_типу_функции(функция.тип)
 
 		if описание.is_empty():
 			return {
-				"ошибка": "Компилятор не знает функцию типа %s." % функция["тип"]
+				"ошибка": "Компилятор не знает функцию типа %s." % функция.тип
 			}
 
 		var результат: Dictionary = закодировать_функцию(функция, описание)
 
 		if результат.has("ошибка"):
-			return результат
+			return {
+				"ошибка": "Функция %d: %s" % [номер, результат["ошибка"]]
+			}
 
 		геном.append_array(результат["байты"])
 
@@ -33,14 +35,14 @@ static func скомпилировать(описание_клетки: Array[Di
 	}
 
 
-static func закодировать_функцию(функция: Dictionary, описание: Dictionary) -> Dictionary:
+static func закодировать_функцию(функция: ВыраженнаяФункция, описание: Dictionary) -> Dictionary:
 	var байты := PackedByteArray()
 	байты.append(описание["маркер"])
 
 	for поле: Dictionary in описание["поля"]:
-		var имя: String = поле["имя"]
+		var имя := StringName(поле["имя"])
 
-		if not функция.has(имя):
+		if not функция.содержит_поле(имя):
 			return {
 				"ошибка": "Для функции «%s» не задан параметр «%s»." % [
 					описание["название"],
@@ -48,67 +50,98 @@ static func закодировать_функцию(функция: Dictionary,
 				]
 			}
 
-		var масштаб: float = float(поле["масштаб"])
-		var минимум: float = float(поле["минимум"])
+		var значение: Variant = функция.получить_значение_поля(имя)
+		var результат_поля: Dictionary = закодировать_поле(значение, поле, описание)
 
-		match поле["тип_данных"]:
-			КаноническаяПрограммаЭкспрессии.ТИП_UINT16:
-				var значение: float = float(функция[имя])
+		if результат_поля.has("ошибка"):
+			return результат_поля
 
-				if значение < минимум:
-					return {
-						"ошибка": "Параметр «%s» функции «%s» меньше допустимого значения." % [
-							поле["название"],
-							описание["название"]
-						]
-					}
+		байты.append_array(результат_поля["байты"])
+
+	return {
+		"байты": байты
+	}
 
-				var сырое_значение: int = roundi(значение * масштаб)
 
-				if сырое_значение > 0xFFFF:
-					return {
-						"ошибка": "Параметр «%s» функции «%s» не помещается в uint16." % [
-							поле["название"],
-							описание["название"]
-						]
-					}
+static func закодировать_поле(значение: Variant, поле: Dictionary, описание: Dictionary) -> Dictionary:
+	var масштаб: float = float(поле["масштаб"])
+	var минимум: float = float(поле["минимум"])
 
-				байты.append((сырое_значение >> 8) & 0xFF)
-				байты.append(сырое_значение & 0xFF)
+	match поле["тип_данных"]:
+		КаноническаяПрограммаЭкспрессии.ТИП_UINT16:
+			var тип_значения: int = typeof(значение)
+
+			if тип_значения != TYPE_INT and тип_значения != TYPE_FLOAT:
+				return ошибка_типа(поле, описание, "число")
+
+			var число: float = float(значение)
+
+			if not is_finite(число):
+				return ошибка_диапазона(поле, описание, "должен быть конечным числом")
+
+			if число < минимум:
+				return ошибка_диапазона(поле, описание, "меньше допустимого значения %s" % минимум)
+
+			var сырое_значение: int = roundi(число * масштаб)
+
+			if сырое_значение < 0 or сырое_значение > 0xFFFF:
+				return ошибка_диапазона(поле, описание, "не помещается в uint16")
+
+			return {
+				"байты": PackedByteArray([
+					(сырое_значение >> 8) & 0xFF,
+					сырое_значение & 0xFF
+				])
+			}
+
+		КаноническаяПрограммаЭкспрессии.ТИП_VECTOR3_UINT16:
+			if typeof(значение) != TYPE_VECTOR3:
+				return ошибка_типа(поле, описание, "Vector3")
+
+			var вектор: Vector3 = значение
+
+			if not вектор.is_finite():
+				return ошибка_диапазона(поле, описание, "должен содержать только конечные компоненты")
 
-			КаноническаяПрограммаЭкспрессии.ТИП_VECTOR3_UINT16:
-				var значение: Vector3 = функция[имя]
+			if вектор.x < минимум or вектор.y < минимум or вектор.z < минимум:
+				return ошибка_диапазона(поле, описание, "содержит компонент меньше %s" % минимум)
 
-				if значение.x < минимум or значение.y < минимум or значение.z < минимум:
-					return {
-						"ошибка": "Компоненты параметра «%s» функции «%s» должны быть не меньше %s." % [
-							поле["название"],
-							описание["название"],
-							минимум
-						]
-					}
+			var байты := PackedByteArray()
+
+			for компонент: float in [вектор.x, вектор.y, вектор.z]:
+				var сырое_значение: int = roundi(компонент * масштаб)
+
+				if сырое_значение < 0 or сырое_значение > 0xFFFF:
+					return ошибка_диапазона(поле, описание, "содержит компонент, не помещающийся в uint16")
+
+				байты.append((сырое_значение >> 8) & 0xFF)
+				байты.append(сырое_значение & 0xFF)
 
-				var компоненты: Array[float] = [значение.x, значение.y, значение.z]
+			return {
+				"байты": байты
+			}
 
-				for компонент: float in компоненты:
-					var сырое_значение: int = roundi(компонент * масштаб)
+		_:
+			return {
+				"ошибка": "Неизвестный тип данных параметра «%s»." % поле["название"]
+			}
 
-					if сырое_значение > 0xFFFF:
-						return {
-							"ошибка": "Компонент параметра «%s» функции «%s» не помещается в uint16." % [
-								поле["название"],
-								описание["название"]
-							]
-						}
 
-					байты.append((сырое_значение >> 8) & 0xFF)
-					байты.append(сырое_значение & 0xFF)
+static func ошибка_типа(поле: Dictionary, описание: Dictionary, ожидаемый_тип: String) -> Dictionary:
+	return {
+		"ошибка": "Параметр «%s» функции «%s» должен иметь тип %s." % [
+			поле["название"],
+			описание["название"],
+			ожидаемый_тип
+		]
+	}
 
-			_:
-				return {
-					"ошибка": "Неизвестный тип данных параметра «%s»." % поле["название"]
-				}
 
+static func ошибка_диапазона(поле: Dictionary, описание: Dictionary, причина: String) -> Dictionary:
 	return {
-		"байты": байты
+		"ошибка": "Параметр «%s» функции «%s» %s." % [
+			поле["название"],
+			описание["название"],
+			причина
+		]
 	}
diff --git a/клетка/компоновщик_клетки.gd b/клетка/компоновщик_клетки.gd
index 8390ff5..7e57c49 100644
--- a/клетка/компоновщик_клетки.gd
+++ b/клетка/компоновщик_клетки.gd
@@ -5,15 +5,15 @@ const ЗаконыКлетки = preload("res://клетка/законы_кле
 const КонструкцияКлеткиКласс = preload("res://клетка/конструкция_клетки.gd")
 
 
-static func собрать(модули: Array[Dictionary], органика_тела_на_модуль: float) -> КонструкцияКлетки:
+static func собрать(модули: Array[ВыраженнаяФункция], органика_тела_на_модуль: float) -> КонструкцияКлетки:
 	var конструкция := КонструкцияКлеткиКласс.new() as КонструкцияКлетки	
 	конструкция.органика_тела_на_модуль = органика_тела_на_модуль
 	
-	for модуль: Dictionary in модули:
+	for модуль: ВыраженнаяФункция in модули:
 		конструкция.модули.append(модуль)
 
-		var органика: float = модуль["органика"]
-		var тип: int = модуль["тип"]
+		var органика: float = модуль.органика
+		var тип: int = модуль.тип
 
 		конструкция.органика_модулей += органика
 
@@ -25,7 +25,7 @@ static func собрать(модули: Array[Dictionary], органика_т
 				добавить_накопитель_энергии(
 					конструкция.профиль,
 					органика,
-					модуль["коэффициент_утечки"]
+					float(модуль.получить_параметр(&"коэффициент_утечки", 0.0))
 				)
 
 			КодыГенов.БИОСИНТЕЗАТОР:
diff --git a/клетка/конструкция_клетки.gd b/клетка/конструкция_клетки.gd
index 8194c84..102aa0c 100644
--- a/клетка/конструкция_клетки.gd
+++ b/клетка/конструкция_клетки.gd
@@ -1,7 +1,7 @@
 class_name КонструкцияКлетки
 extends RefCounted
 
-var модули: Array[Dictionary] = []
+var модули: Array[ВыраженнаяФункция] = []
 var профиль: ПрофильФункций = ПрофильФункций.new()
 
 var органика_тела_на_модуль: float = 0.0
diff --git a/клетка/сборка_клетки.gd b/клетка/сборка_клетки.gd
index c80f466..bc9a21b 100644
--- a/клетка/сборка_клетки.gd
+++ b/клетка/сборка_клетки.gd
@@ -3,6 +3,6 @@ extends RefCounted
 
 var геном_hex: String = ""
 var полный_геном: PackedByteArray = PackedByteArray()
-var доступные_участки: Array[Dictionary] = []
+var доступные_участки: Array[Vector2i] = []
 var конструкция: КонструкцияКлетки
 var морфология: МорфологияКлетки
diff --git a/клетка/сборщик_клетки.gd b/клетка/сборщик_клетки.gd
index 61e4dcd..d91049e 100644
--- a/клетка/сборщик_клетки.gd
+++ b/клетка/сборщик_клетки.gd
@@ -48,10 +48,19 @@ static func собрать_из_генома(геном: PackedByteArray, дос
 			"ошибка": "Геном не должен быть пустым."
 		}
 
+	var результат_нормализации: Dictionary = ГенетическийИнтерпретатор.нормализовать_участки(
+		геном.size(),
+		доступные_участки
+	)
+
+	if результат_нормализации.has("ошибка"):
+		return результат_нормализации
+
+	var нормализованные_участки: Array[Vector2i] = результат_нормализации["участки"]
 	var геном_hex: String = HexКодек.байты_в_hex(геном)
-	var ключ_сборки: String = создать_ключ_сборки(геном_hex, доступные_участки)
+	var ключ_сборки: String = создать_ключ_сборки(геном_hex, нормализованные_участки)
 
-	if not ключ_сборки.is_empty() and кэш_сборок.has(ключ_сборки):
+	if кэш_сборок.has(ключ_сборки):
 		попадания_в_кэш += 1
 		return {
 			"сборка": кэш_сборок[ключ_сборки]
@@ -59,15 +68,15 @@ static func собрать_из_генома(геном: PackedByteArray, дос
 
 	промахи_кэша += 1
 
-	var результат_экспрессии: Dictionary = ГенетическийИнтерпретатор.выразить(
+	var результат_экспрессии: Dictionary = ГенетическийИнтерпретатор.выразить_из_нормализованных_участков(
 		геном,
-		доступные_участки
+		нормализованные_участки
 	)
 
 	if результат_экспрессии.has("ошибка"):
 		return результат_экспрессии
 
-	var модули: Array[Dictionary] = результат_экспрессии["выраженные_функции"]
+	var модули: Array[ВыраженнаяФункция] = результат_экспрессии["выраженные_функции"]
 
 	if модули.is_empty():
 		return {
@@ -91,11 +100,10 @@ static func собрать_из_генома(геном: PackedByteArray, дос
 	сборка.конструкция = конструкция
 	сборка.морфология = морфология
 
-	for участок: Dictionary in доступные_участки:
-		сборка.доступные_участки.append(участок.duplicate(true))
+	for участок: Vector2i in нормализованные_участки:
+		сборка.доступные_участки.append(участок)
 
-	if not ключ_сборки.is_empty():
-		кэш_сборок[ключ_сборки] = сборка
+	кэш_сборок[ключ_сборки] = сборка
 
 	return {
 		"сборка": сборка
@@ -121,17 +129,11 @@ static func получить_статистику_кэшей() -> Dictionary:
 	}
 
 
-static func создать_ключ_сборки(геном_hex: String, доступные_участки: Array[Dictionary]) -> String:
+static func создать_ключ_сборки(геном_hex: String, доступные_участки: Array[Vector2i]) -> String:
 	var части := PackedStringArray()
 	части.append(геном_hex)
 
-	for участок: Dictionary in доступные_участки:
-		if not участок.has("начало_бита") or not участок.has("длина_бит"):
-			return ""
-
-		части.append("%d:%d" % [
-			int(участок["начало_бита"]),
-			int(участок["длина_бит"])
-		])
+	for участок: Vector2i in доступные_участки:
+		части.append("%d:%d" % [участок.x, участок.y])
 
 	return "|".join(части)
diff --git a/клетка/форма_клетки.gd b/клетка/форма_клетки.gd
index 7a7eea2..651bd13 100644
--- a/клетка/форма_клетки.gd
+++ b/клетка/форма_клетки.gd
@@ -21,7 +21,7 @@ static var направления_упаковки: Array[Vector3] = []
 class ДанныеПостроения:
 	extends RefCounted
 
-	var модули: Array[Dictionary] = []
+	var модули: Array[ВыраженнаяФункция] = []
 	var органика_тела_на_модуль: float = 0.0
 
 	var типы: PackedInt32Array = PackedInt32Array()
@@ -82,10 +82,10 @@ static func получить_статистику_кэша() -> Dictionary:
 static func создать_ключ_морфологии(конструкция: КонструкцияКлетки) -> String:
 	var данные_ключа: Array = [конструкция.органика_тела_на_модуль]
 
-	for модуль: Dictionary in конструкция.модули:
-		var пропорции: Vector3 = модуль["пропорции"]
-		данные_ключа.append(int(модуль["тип"]))
-		данные_ключа.append(float(модуль["органика"]))
+	for модуль: ВыраженнаяФункция in конструкция.модули:
+		var пропорции: Vector3 = модуль.пропорции
+		данные_ключа.append(модуль.тип)
+		данные_ключа.append(модуль.органика)
 		данные_ключа.append(пропорции)
 
 	return Marshalls.raw_to_base64(var_to_bytes(данные_ключа))
@@ -183,9 +183,9 @@ static func подготовить_данные_построения(конст
 	данные.условные_радиусы.resize(количество)
 
 	for номер: int in range(количество):
-		var модуль: Dictionary = данные.модули[номер]
-		var тип: int = int(модуль["тип"])
-		var органика_формы: float = float(модуль["органика"]) + данные.органика_тела_на_модуль
+		var модуль: ВыраженнаяФункция = данные.модули[номер]
+		var тип: int = модуль.тип
+		var органика_формы: float = модуль.органика + данные.органика_тела_на_модуль
 		var базовый_радиус: float = pow(органика_формы, 1.0 / 3.0)
 		var размеры: Vector3 = получить_размеры_модуля(модуль, базовый_радиус)
 
@@ -299,8 +299,8 @@ static func место_свободно(номер: int, место: Vector3, р
 	return true
 
 
-static func получить_размеры_модуля(модуль: Dictionary, базовый_радиус: float) -> Vector3:
-	var пропорции: Vector3 = модуль["пропорции"]
+static func получить_размеры_модуля(модуль: ВыраженнаяФункция, базовый_радиус: float) -> Vector3:
+	var пропорции: Vector3 = модуль.пропорции
 	var нормализатор: float = pow(пропорции.x * пропорции.y * пропорции.z, 1.0 / 3.0)
 	return пропорции / нормализатор * базовый_радиус
 
diff --git a/тесты/r0_фиксация_поведения.gd b/тесты/r0_фиксация_поведения.gd
index 751f7d0..ee834c2 100644
--- a/тесты/r0_фиксация_поведения.gd
+++ b/тесты/r0_фиксация_поведения.gd
@@ -53,7 +53,7 @@ func выполнить_проверку() -> void:
 		завершить_отчет()
 		return
 
-	var модули: Array[Dictionary] = результат_экспрессии["выраженные_функции"]
+	var модули: Array[ВыраженнаяФункция] = результат_экспрессии["выраженные_функции"]
 
 	вывести_генетический_отчет(геном, модули)
 	проверить_компиляцию(геном, модули)
@@ -76,7 +76,7 @@ func выразить_весь_геном(геном: PackedByteArray) -> Dictio
 	return ГенетическийИнтерпретатор.выразить(геном, доступные_участки)
 
 
-func вывести_генетический_отчет(геном: PackedByteArray, модули: Array[Dictionary]) -> void:
+func вывести_генетический_отчет(геном: PackedByteArray, модули: Array[ВыраженнаяФункция]) -> void:
 	print("\n--- Геном и экспрессия ---")
 	print("Байт в геноме: %d" % геном.size())
 	print("Выраженных функций: %d" % модули.size())
@@ -92,10 +92,12 @@ func вывести_генетический_отчет(геном: PackedByteAr
 
 	проверить_целое(геном.size(), 83, "Размер эталонного генома")
 	проверить_целое(модули.size(), 9, "Количество выраженных функций")
+	проверить(модули[0] is ВыраженнаяФункция, "Интерпретатор возвращает типизированные функции")
+	проверить_целое(модули[0].участки_генома.size(), 1, "Функция содержит типизированный участок генома")
 	проверить_целое(посчитать_тип(модули, КодыГенов.ПОЧКОВАНИЕ), 3, "Количество функций почкования")
 
 
-func проверить_компиляцию(геном: PackedByteArray, модули: Array[Dictionary]) -> void:
+func проверить_компиляцию(геном: PackedByteArray, модули: Array[ВыраженнаяФункция]) -> void:
 	print("\n--- Повторная компиляция ---")
 
 	var результат: Dictionary = КаноническийКомпиляторГенома.скомпилировать(модули)
@@ -110,6 +112,21 @@ func проверить_компиляцию(геном: PackedByteArray, мод
 	print("Компиляция выраженных функций возвращает исходный геном: %s" % совпадает)
 	проверить(совпадает, "Двоичный геном после повторной компиляции не изменился")
 
+	var некорректные: Array[ВыраженнаяФункция] = скопировать_модули(модули)
+
+	for функция: ВыраженнаяФункция in некорректные:
+		if функция.тип == КодыГенов.НАКОПИТЕЛЬ_ЭНЕРГИИ:
+			функция.установить_параметр(&"коэффициент_утечки", "ошибка")
+			break
+
+	var результат_ошибки_типа: Dictionary = КаноническийКомпиляторГенома.скомпилировать(некорректные)
+	проверить(результат_ошибки_типа.has("ошибка"), "Компилятор контролирует тип специфического параметра")
+
+	var вне_диапазона: Array[ВыраженнаяФункция] = скопировать_модули(модули)
+	вне_диапазона[0].органика = 100.0
+	var результат_диапазона: Dictionary = КаноническийКомпиляторГенома.скомпилировать(вне_диапазона)
+	проверить(результат_диапазона.has("ошибка"), "Компилятор контролирует диапазон uint16")
+
 
 func проверить_сборщик_клетки() -> void:
 	print("\n--- Единый сборщик клетки и кэши ---")
@@ -172,10 +189,8 @@ func проверить_сборщик_клетки() -> void:
 		зарегистрировать_ошибку("Сборка с повторёнными участками завершилась ошибкой: %s" % результат_повторенных_участков["ошибка"])
 	else:
 		var сборка_повторенных_участков: СборкаКлетки = результат_повторенных_участков["сборка"]
-		проверить(not is_same(сборка_повторенных_участков, сборка), "Другой ключ экспрессии создаёт отдельную сборку")
-		проверить(not is_same(сборка_повторенных_участков.конструкция, конструкция), "Отдельная сборка получает отдельную конструкцию")
-		проверить(is_same(сборка_повторенных_участков.морфология, сборка.морфология), "Одинаковый фенотип разделяет морфологию")
-		проверить(is_same(сборка_повторенных_участков.морфология.меш, сборка.морфология.меш), "Одинаковый фенотип разделяет меш")
+		проверить(is_same(сборка_повторенных_участков, сборка), "Повторённые участки нормализуются в тот же ключ сборки")
+		проверить_целое(сборка_повторенных_участков.доступные_участки.size(), 1, "Повторённые участки объединены")
 
 	var ограниченные_участки: Array[Dictionary] = [{
 		"начало_бита": 0,
@@ -228,7 +243,7 @@ func проверить_участки_и_повреждения(геном: Pac
 	if результат_72.has("ошибка"):
 		зарегистрировать_ошибку("Экспрессия первых 72 бит: %s" % результат_72["ошибка"])
 	else:
-		var функции_72: Array[Dictionary] = результат_72["выраженные_функции"]
+		var функции_72: Array[ВыраженнаяФункция] = результат_72["выраженные_функции"]
 		print("Функций из первых 72 бит: %d" % функции_72.size())
 		проверить_целое(функции_72.size(), 1, "Первые 72 бита выражают одну функцию")
 
@@ -240,7 +255,7 @@ func проверить_участки_и_повреждения(геном: Pac
 	if результат_повреждения.has("ошибка"):
 		зарегистрировать_ошибку("Повреждённый хвост: %s" % результат_повреждения["ошибка"])
 	else:
-		var поврежденные_функции: Array[Dictionary] = результат_повреждения["выраженные_функции"]
+		var поврежденные_функции: Array[ВыраженнаяФункция] = результат_повреждения["выраженные_функции"]
 		print("Функций после удаления последнего байта: %d" % поврежденные_функции.size())
 		print("Функций почкования после повреждения: %d" % посчитать_тип(поврежденные_функции, КодыГенов.ПОЧКОВАНИЕ))
 
@@ -255,12 +270,12 @@ func проверить_участки_и_повреждения(геном: Pac
 	if результат_мусора.has("ошибка"):
 		зарегистрировать_ошибку("Мусорный хвост: %s" % результат_мусора["ошибка"])
 	else:
-		var функции_с_мусором: Array[Dictionary] = результат_мусора["выраженные_функции"]
+		var функции_с_мусором: Array[ВыраженнаяФункция] = результат_мусора["выраженные_функции"]
 		print("Функций после добавления неизвестных байтов: %d" % функции_с_мусором.size())
 		проверить_целое(функции_с_мусором.size(), 9, "Неизвестные байты не изменяют предыдущую экспрессию")
 
 
-func вывести_статические_характеристики(модули: Array[Dictionary]) -> void:
+func вывести_статические_характеристики(модули: Array[ВыраженнаяФункция]) -> void:
 	print("\n--- Статические характеристики ---")
 
 	var конструкция: КонструкцияКлетки = КомпоновщикКлетки.собрать(
@@ -310,7 +325,7 @@ func вывести_статические_характеристики(моду
 	проверить_близко(профиль.емкость_термата, 5.196152423, "Ёмкость термата")
 
 
-func вывести_результат_одного_такта(модули: Array[Dictionary]) -> void:
+func вывести_результат_одного_такта(модули: Array[ВыраженнаяФункция]) -> void:
 	print("\n--- Состояние после одного такта delta=1.0 ---")
 
 	var конструкция: КонструкцияКлетки = КомпоновщикКлетки.собрать(
@@ -395,10 +410,10 @@ func вывести_геометрию() -> void:
 		проверить_вектор_близко(точки_сцепки[2], Vector3(0.201492503, -3.829691648, 0.401774049), "Точка сцепки 2", 0.0001)
 
 
-func проверить_повторения_функций(модули: Array[Dictionary]) -> void:
+func проверить_повторения_функций(модули: Array[ВыраженнаяФункция]) -> void:
 	print("\n--- Повторения функций 01–06 ---")
 
-	var модули_с_повторами: Array[Dictionary] = скопировать_модули(модули)
+	var модули_с_повторами: Array[ВыраженнаяФункция] = скопировать_модули(модули)
 
 	добавить_копию_типа(модули_с_повторами, модули, КодыГенов.ПОГЛОТИТЕЛЬ_ЭНЕРГИИ)
 	добавить_копию_типа(модули_с_повторами, модули, КодыГенов.НАКОПИТЕЛЬ_ЭНЕРГИИ)
@@ -422,7 +437,7 @@ func проверить_повторения_функций(модули: Array[
 		зарегистрировать_ошибку("Экспрессия повторений: %s" % результат_экспрессии["ошибка"])
 		return
 
-	var выраженные_функции: Array[Dictionary] = результат_экспрессии["выраженные_функции"]
+	var выраженные_функции: Array[ВыраженнаяФункция] = результат_экспрессии["выраженные_функции"]
 	var конструкция: КонструкцияКлетки = КомпоновщикКлетки.собрать(
 		выраженные_функции,
 		ОРГАНИКА_ТЕЛА_НА_МОДУЛЬ
@@ -460,29 +475,29 @@ func проверить_повторения_функций(модули: Array[
 	проверить_целое(профиль.количество_мест_почкования, 3, "Количество мест почкования не изменилось")
 
 
-func скопировать_модули(модули: Array[Dictionary]) -> Array[Dictionary]:
-	var копии: Array[Dictionary] = []
+func скопировать_модули(модули: Array[ВыраженнаяФункция]) -> Array[ВыраженнаяФункция]:
+	var копии: Array[ВыраженнаяФункция] = []
 
-	for модуль: Dictionary in модули:
-		копии.append(модуль.duplicate(true))
+	for модуль: ВыраженнаяФункция in модули:
+		копии.append(модуль.дублировать())
 
 	return копии
 
 
-func добавить_копию_типа(приемник: Array[Dictionary], источник: Array[Dictionary], тип: int) -> void:
-	for модуль: Dictionary in источник:
-		if int(модуль["тип"]) == тип:
-			приемник.append(модуль.duplicate(true))
+func добавить_копию_типа(приемник: Array[ВыраженнаяФункция], источник: Array[ВыраженнаяФункция], тип: int) -> void:
+	for модуль: ВыраженнаяФункция in источник:
+		if модуль.тип == тип:
+			приемник.append(модуль.дублировать())
 			return
 
 	зарегистрировать_ошибку("Не найден модуль типа %02X для повторения." % тип)
 
 
-func посчитать_тип(модули: Array[Dictionary], тип: int) -> int:
+func посчитать_тип(модули: Array[ВыраженнаяФункция], тип: int) -> int:
 	var количество: int = 0
 
-	for модуль: Dictionary in модули:
-		if int(модуль["тип"]) == тип:
+	for модуль: ВыраженнаяФункция in модули:
+		if модуль.тип == тип:
 			количество += 1
 
 	return количество
